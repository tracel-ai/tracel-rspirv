name: publish

on:
  push:
    tags:
      - "v*"

jobs:
  publish-tracel-rspirv:
    uses: tracel-ai/github-actions/.github/workflows/publish-crate.yml@v4
    with:
      crate: tracel-rspirv
    secrets: inherit
